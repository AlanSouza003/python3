from math import sqrt

# Aqui conseguimos simplificar mais o código. Ao invés de colocarmos o math.sqrt, colocamos somente
# o sqrt, sem precisar colocar o math antes.

# Ex:

# num = int(input('Digite um número: '))
# raiz = sqrt(num)
# print('A raiz quadrada de {} é {}!'.format(num, raiz))

# Dessa forma economizamos mais tempo e linha e deixamos o código mais limpo.